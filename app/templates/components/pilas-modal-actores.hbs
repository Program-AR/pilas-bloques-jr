{{yield}}

{{#ember-remodal forService=true name='pilas-modal-actores' title="Seleccioná un actor"}}

  {{#each clasesDeActores as |clase|}}
    <img src="{{clase.thumbPath}}" class="actor-thumb" {{action "seleccionar" clase}}>
  {{/each}}

{{/ember-remodal}}
